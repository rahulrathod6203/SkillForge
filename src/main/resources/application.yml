spring :
  application :
    name : SkillForge

    # Database configuration
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  #JPA Hibernate configuration
  jpa:
    hibernate:
      ddl-auto: update
